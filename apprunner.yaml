version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo Hello There!!
run:
  runtime-version: 3.11
  pre-run:
    - echo Beginning pre-run commands...
    - pip3 install pipenv
    - pipenv install
    - echo Done with pre-run!!!
  command: pipenv run fastapi run backend/backend.py --port 8080

  network:
    port: 8080
