version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo Hello There!!
run:
  runtime-version: 3.11
  pre-run:
    - pip3 install pipenv
    - pipenv install
  command: pipenv run uvicorn backend.backend:app --host 0.0.0.0 --port 8000